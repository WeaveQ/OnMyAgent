/** @jsxImportSource react */
/**
 * Session workspace route entry — orchestration only.
 * Implementation lives in `session-route-render.tsx` (controller + view).
 * Helpers: session-route-*.ts / session-route-intent.ts
 */
export { SessionRouteRender as SessionRoute } from "./session-route-render";
