/**
 * Projects is a coming-soon placeholder. Hide the primary-rail entry until
 * the surface ships. Deep links / bookmarks fall back to Home.
 */
export function isProjectsRailVisible(): boolean {
  return false;
}
