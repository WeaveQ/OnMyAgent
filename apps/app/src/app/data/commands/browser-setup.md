---
name: browser-setup
description: Use the built-in OnMyAgent browser
---

Try browser automation in OnMyAgent right away.

IMPORTANT:

- Use the built-in OnMyAgent browser tools.
- In your first response, open `https://example.com` and tell the user the page title.
- If browser tools are unavailable, tell the user browser control is not available in this workspace and ask them to retry after OnMyAgent reloads.

Keep the response short and action-oriented.
