import { APP_NAME } from "../brand";

export default {
  "debug.reset_counters": "Reset counters",
  "debug.collapse": "Collapse",
  "debug.hide_profiler": "Hide (Cmd+Shift+P to toggle)",
  "debug.hide_render_watchdog": "Hide (Cmd+Shift+L to toggle)",
} as const;
