# 物流单录入作业 · 物流单专家

把客户发来的微信文字、语音、图片或单据模板发给我，我会先确认模板，再用 Tab 实时展示白、红、黄三联物流单效果（过程稿在 `.process/`）。你补充一次，三联就同步完善一次；确认后由你选择是否生成 PDF/Excel。

## 类型

Agent 型（单个 AI 专家）

## 功能

以资深真人调度员经验为蓝本，解决"订单信息碎片化、录单慢、易错漏，信息不全导致反复确认"的痛点：

- **多模态识别**：客户常发语音或拍照，支持文字、语音转写、手写单/便签照片识别
- **模板优先**：先问是否有指定模板，有就按模板生成，无就按通用格式生成
- **真实三联效果**：对话中同步更新白色存根联、红色收货单位联、黄色发货单位联三个独立 HTML
- **过程/结果分离**：预览 HTML 放在 `.process/`，PDF/XLSX 直接写在会话根目录（不套 `output/`）
- **预览内改字段**：可手动编辑格子并保存，回写数据后刷新预览；旧结果产物自动作废
- **会话内 Tab 预览**：当前物流单直接嵌入对话正文，默认白色存根联，可通过 Tab 切换红联和黄联
- **按需导出**：确认后选择「PDF+Excel / 仅 PDF / 仅 Excel / 先不生成」；变更数据会先删旧文件再重生
- **单页 PDF**：物流单据按 A4 横向单页导出，不分页
- **缺失检查与智能追问**：对照必填字段逐项检查，生成礼貌、一次问清、可直接转发的追问话术
- **状态门禁**：车辆/司机未齐只生成“待派车确认稿”，派车信息齐全并确认后才生成“最终版”

附带 `order-entry` 知识 Skill，内含字段协议、状态机、固定 HTML 模板、追问话术与确定性 PDF/XLSX 导出脚本。

## 使用示例

- 客户微信发来："明天上午从深圳发两台注塑机到东莞，收货人刘经理，要尾板车，越快越好"，帮我生成物流单并逐步补全效果图
- 这张手写发货单照片有些潦草，帮我识别内容，并列出还需要向客户确认哪些信息
- 帮我做一张派车单，先问我有没有公司模板，没有就按通用格式
- 帮我汇总今天收到的所有订单，检查有没有漏单或信息不全的，生成待追问清单

## 头像

头像已生成在 `avatars/expert.png`：图标式扁平设计（非人形），深蓝物流底色 + 运单文档 + 语音气泡 + 校验徽章 + 货车元素，体现"消息进单 → 结构化 → 校验追问 → 发运"的完整能力链路。如需替换为自定义头像，要求：

- 格式：PNG（推荐）或 JPG
- 尺寸：512×512 px
- 大小：单张不超过 500KB
