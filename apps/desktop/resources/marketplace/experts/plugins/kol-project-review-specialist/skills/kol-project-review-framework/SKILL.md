---
name: kol-project-review-framework
description: 根据项目数据、执行过程、客户反馈输出完整复盘框架（背景、目标完成、亮点、问题、原因、优化建议）。当用户说“帮我写复盘”“出个复盘框架”“结案总结”时使用。
---

# 项目复盘框架生成

生成可直接用于内部分享或结案讨论的复盘骨架与内容。

## 输入

- 项目目标、执行记录、数据表、客户反馈；
- 用户复盘模板或往期复盘示例（可选，**优先**）。

## 处理

默认结构：项目背景 → 目标完成情况 → 亮点 → 问题 → 原因分析 → 下次优化建议。  
有用户模板则严格套模板。数据不足处标注“待补”，不虚构结果。

## 输出

- 完整复盘文稿/框架；
- 需补数据清单；
- 可选导出文档。

## 边界

- 不做毛利三层精算细节（可衔接毛利与效果分析技能）
- 不替用户对外发送结案报告


## 模板优先（强制）

1. 用户提供了模板文件、示例文件，或在对话中给出示例结构 → **严格按用户结构、字段顺序、标题与表述风格输出**；输出开头标注：`已严格按您提供的模板/示例输出`。
2. 用户未提供任何模板/示例 → 使用本技能默认最佳实践结构；可标注：`按默认最佳实践输出`。
3. 用户模板与内部模板冲突时，**用户提供的永远优先**。
4. 多模态输入（PDF / Word / Excel / 图片截图等）先完成解析与理解，再进入上述逻辑。

## 文件交付

- 需要导出业务表/文档时：用 `document-processing` + spreadsheets `write-xlsx` / `write` **真正写入**会话目录并 `verify`，产物卡才会出现。
- **跟进轮同样必须真写入**；禁止只在正文宣称「已生成」；禁止 `文件路径：`；临时 JSON 只放 `os.tmpdir()` / `.opencode/tmp/`。
- 正文用粗体标文件名；先结论后展开。
