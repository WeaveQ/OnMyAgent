---
name: kol-margin-effect-analysis
description: 按收入、K金额、广告金额、返点、毛利率等做纯K前端/投流潜力/总投入效果三层分析，输出结论与风险。当用户说“算一下毛利”“看项目效果”“结案效果分析”时使用。
---

# 项目毛利与效果分析

用可量化指标做分层效果与毛利解读。

## 输入

- 项目收入、K 费用、投流/广告费、返点、其他成本；
- 效果数据（曝光、互动、转化等，有则用）；
- 用户分析模板（可选，**优先**）。

## 处理（默认三层）

1. **纯 K 前端**：内容合作本身的投入产出与毛利结构  
2. **投流潜力**：可放大投放的内容/达人信号  
3. **总投入效果**：K+广告+其他成本后的综合效果与风险  

公式与数字必须标明来源；缺数则列假设与待补，不装精确。

## 输出

- 三层结论表；
- 毛利/效果关键指标；
- 风险提示与优化方向。

## 边界

- 不替代财务正式核算与税务处理
- 不编造转化/ROI


## 模板优先（强制）

1. 用户提供了模板文件、示例文件，或在对话中给出示例结构 → **严格按用户结构、字段顺序、标题与表述风格输出**；输出开头标注：`已严格按您提供的模板/示例输出`。
2. 用户未提供任何模板/示例 → 使用本技能默认最佳实践结构；可标注：`按默认最佳实践输出`。
3. 用户模板与内部模板冲突时，**用户提供的永远优先**。
4. 多模态输入（PDF / Word / Excel / 图片截图等）先完成解析与理解，再进入上述逻辑。

## 文件交付

- 需要导出业务表/文档时：用 `document-processing` + spreadsheets `write-xlsx` / `write` **真正写入**会话目录并 `verify`，产物卡才会出现。
- **跟进轮同样必须真写入**；禁止只在正文宣称「已生成」；禁止 `文件路径：`；临时 JSON 只放 `os.tmpdir()` / `.opencode/tmp/`。
- 正文用粗体标文件名；先结论后展开。
