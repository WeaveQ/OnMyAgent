---
name: kol-data-clean-merge
description: 结案复盘前合并清洗项目费用/效果等多源数据表（去重、字段对齐、异常值、格式统一），并给出可写复盘的基础指标。当用户说“合一下复盘数据”“清洗项目效果表”“结案前把表整理好”时使用。
---

# 复盘数据准备（清洗与合并）

把复盘相关多表整理成可写结案的干净表，并做基础指标汇总。侧重项目费用、效果、执行结果数据，不是达人提报池整理。

## 输入

- 多份 CSV/Excel/粘贴表；
- 主键/对齐字段说明（可选）；
- 用户目标字段或示例表（可选，**优先**）。

## 处理

1. 字段对齐与重命名映射  
2. 去重规则（明确主键；冲突保留并标注）  
3. 异常值、空值、格式统一（日期、金额、百分比）  
4. 基础可量化指标（条数、求和、均值、占比等，按用户诉求）  
5. 导出清洗结果表（真写入）

## 输出

- 清洗后主表；
- 清洗日志（去重/异常/映射说明）；
- 基础指标摘要。

## 边界

- 不做复杂归因建模或未授权的外部数据抓取
- 无依据不编造指标


## 模板优先（强制）

1. 用户提供了模板文件、示例文件，或在对话中给出示例结构 → **严格按用户结构、字段顺序、标题与表述风格输出**；输出开头标注：`已严格按您提供的模板/示例输出`。
2. 用户未提供任何模板/示例 → 使用本技能默认最佳实践结构；可标注：`按默认最佳实践输出`。
3. 用户模板与内部模板冲突时，**用户提供的永远优先**。
4. 多模态输入（PDF / Word / Excel / 图片截图等）先完成解析与理解，再进入上述逻辑。

## 文件交付

- 需要导出业务表/文档时：用 `document-processing` + spreadsheets `write-xlsx` / `write` **真正写入**会话目录并 `verify`，产物卡才会出现。
- **跟进轮同样必须真写入**；禁止只在正文宣称「已生成」；禁止 `文件路径：`；临时 JSON 只放 `os.tmpdir()` / `.opencode/tmp/`。
- 正文用粗体标文件名；先结论后展开。
