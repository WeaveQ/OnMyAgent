---
name: kol-reputation-monitor
description: 基于内容/评论数据辅助做声量、情感倾向、负面舆情、高危词监测，输出摘要与人工干预建议。当用户说“看看评论风向”“口碑怎么样”“有没有负面”时使用。
---

# 口碑检测辅助

对已有内容/评论样本做口碑辅助监测，便于运维干预。

## 输入

- 评论导出、截图、内容反馈文本；
- 监测周期与重点达人/作品（可选）；
- 用户监测维度/报告模板（可选，**优先**）。

## 处理（默认）

1. 声量概况  
2. 情感倾向粗分（正/中/负）  
3. 负面与高危词/敏感点  
4. 需人工干预的优先级建议  

样本有限时明确说明局限，不做全网实时监控承诺。

## 输出

- 口碑摘要表；
- 高危/负面条目；
- 干预建议（回复口径方向，不替用户发送）。

## 边界

- 不替代专业舆情系统与危机公关决策
- 不编造评论数据


## 模板优先（强制）

1. 用户提供了模板文件、示例文件，或在对话中给出示例结构 → **严格按用户结构、字段顺序、标题与表述风格输出**；输出开头标注：`已严格按您提供的模板/示例输出`。
2. 用户未提供任何模板/示例 → 使用本技能默认最佳实践结构；可标注：`按默认最佳实践输出`。
3. 用户模板与内部模板冲突时，**用户提供的永远优先**。
4. 多模态输入（PDF / Word / Excel / 图片截图等）先完成解析与理解，再进入上述逻辑。

## 文件交付

- 需要导出业务表/文档时：用 `document-processing` + spreadsheets `write-xlsx` / `write` **真正写入**会话目录并 `verify`，产物卡才会出现。
- **跟进轮同样必须真写入**；禁止只在正文宣称「已生成」；禁止 `文件路径：`；临时 JSON 只放 `os.tmpdir()` / `.opencode/tmp/`。
- 正文用粗体标文件名；先结论后展开。
