---
name: settlement-readiness-audit
description: 按公司结算要求检查回单、费用凭证和审批是否齐全，哪些能结、哪些不能。当用户说“这些单能不能结”“还缺哪些回单凭证”时使用。
---

# 检查每票能不能结账

逐票对照结算要求，避免缺资料就付/收。

## 输入

- 对账表与回单状态；
- 费用凭证、异常关闭、审批；
- 客户/承运商/公司结算规则或模板。

## 处理

1. 列出每票必有项与例外。
2. 核对回单、费用依据、异常关闭、金额确认、审批。
3. 分档 `可以结 / 暂时不能结 / 需要补资料 / 有争议`。
4. 汇总不能结金额、补证责任人、优先处理单。
5. 导出必须工具真写入。

## 输出

- 每票分档与原因；
- 缺什么、找谁补；
- 分类金额与催补清单/话术。

## 文件交付

- **跟进轮同样必须真写入**：用户说「合并/继续/再导出」时，本轮仍要 `write-xlsx`/write，禁止只在正文宣称生成了文件。
- 需要导出业务表时：用 spreadsheets `write-xlsx` / `extract-sheets` 或 write 工具**真正写入**会话目录，并 `verify`；产物卡才会出现。
- **禁止**只在正文写「已生成 xxx.xlsx」却不调用写入工具。
- **禁止**正文以「已生成 **文件名.xlsx**」开场；先结论与要点，再展开。
- 过程脚本与临时 JSON（如 `oma-summary.json`）只放 `os.tmpdir()` / `.opencode/tmp/`，禁止写会话根；不要输出 `文件路径：…`。

## 边界

- 不替用户付款/入账
- 开票资料 → `invoice-information-audit`
- 不派车、不报价
