---
name: charge-variance-audit
description: 对比报价、合同、订单费用与账单，找出少收、多付、漏收、重复和无凭证费用。当用户说“看看哪些金额对不上”“帮我查少收多付”时使用。
---

# 找出哪些金额对不上

结账前找出差额并说明可能原因与缺凭证。

## 输入

- 已整理对账表；
- 合同/报价规则、附加费依据；
- 关注金额阈值或模板（可选）。

## 处理

1. 确认含税、币种、单位、加减项口径。
2. 比较应收、实收/拟收、应付承运、合同价。
3. 查重复、漏附、超合同、无凭证、串票。
4. 分档：有原因 / 需补证 / 需业务确认 / 金额风险；算差额。
5. 差额清单导出必须工具真写入。

## 输出

- 每票差额、可能原因、金额；
- 漏收/重复/无凭证；
- 补证对象与询问话术（可复制）。

## 文件交付

- **跟进轮同样必须真写入**：用户说「合并/继续/再导出」时，本轮仍要 `write-xlsx`/write，禁止只在正文宣称生成了文件。
- 需要导出业务表时：用 spreadsheets `write-xlsx` / `extract-sheets` 或 write 工具**真正写入**会话目录，并 `verify`；产物卡才会出现。
- **禁止**只在正文写「已生成 xxx.xlsx」却不调用写入工具。
- **禁止**正文以「已生成 **文件名.xlsx**」开场；先结论与要点，再展开。
- 过程脚本与临时 JSON（如 `oma-summary.json`）只放 `os.tmpdir()` / `.opencode/tmp/`，禁止写会话根；不要输出 `文件路径：…`。

## 边界

- 不擅自入账或对外扣款
- 不判结算就绪全集 → `settlement-readiness-audit`
- 不派车、不盯在途
