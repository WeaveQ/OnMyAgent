---
name: invoice-information-audit
description: 核对开票抬头、税号、票种、税率、金额并写开票申请初稿。当用户说“帮我检查开票资料”“这些信息能开票吗”时使用。
---

# 检查开票资料并写申请

减少抄错抬头、金额和来回补资料。

## 输入

- 客户开票资料、合同或截图文字；
- 已确认运单与金额；
- 票种、税率、服务名称、交付要求；
- 申请模板（可选）。

## 处理

1. 抽取名称、税号、票种、税率、服务名、地址电话、开户行账号、收票方式。
2. 核对与合同一致、金额合计、含税与税率。
3. 标 `确定 / 可能同一 / 冲突 / 缺少`。
4. 一次列清需业务或客户确认项；申请稿可复制。
5. 导出必须工具真写入。

## 输出

- 整理后的开票信息；
- 冲突与缺项；
- 客户补资料话术；
- 开票申请初稿（用户确认后提交）。

## 文件交付

- **跟进轮同样必须真写入**：用户说「合并/继续/再导出」时，本轮仍要 `write-xlsx`/write，禁止只在正文宣称生成了文件。
- 需要导出业务表时：用 spreadsheets `write-xlsx` / `extract-sheets` 或 write 工具**真正写入**会话目录，并 `verify`；产物卡才会出现。
- **禁止**只在正文写「已生成 xxx.xlsx」却不调用写入工具。
- **禁止**正文以「已生成 **文件名.xlsx**」开场；先结论与要点，再展开。
- 过程脚本与临时 JSON（如 `oma-summary.json`）只放 `os.tmpdir()` / `.opencode/tmp/`，禁止写会话根；不要输出 `文件路径：…`。

## 边界

- 不替用户提交税局/审批系统
- 不派车、不盯在途
