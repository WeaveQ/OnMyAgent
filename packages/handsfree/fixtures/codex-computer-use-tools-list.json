{
  "source": "Sanitized schema projection observed from the local Codex Computer Use plugin 1.0.1000387",
  "tools": [
    { "name": "list_apps", "description": "List the apps on this computer. Returns the set of apps that are currently running, as well as any that have been used in the last 14 days, including details on usage frequency", "required": [], "properties": [], "types": {}, "enums": {}, "readOnly": true },
    { "name": "get_app_state", "description": "Start an app use session if needed, then get the state of the app's key window and return a screenshot and accessibility tree. This must be called once per assistant turn before interacting with the app", "required": ["app"], "properties": ["app"], "types": { "app": "string" }, "enums": {}, "readOnly": true },
    { "name": "click", "description": "Click an element by index or pixel coordinates from screenshot", "required": ["app"], "properties": ["app", "click_count", "element_index", "mouse_button", "x", "y"], "types": { "app": "string", "click_count": "integer", "element_index": "string", "mouse_button": "string", "x": "number", "y": "number" }, "enums": { "mouse_button": ["left", "right", "middle"] }, "readOnly": false },
    { "name": "perform_secondary_action", "description": "Invoke a secondary accessibility action exposed by an element", "required": ["action", "app", "element_index"], "properties": ["action", "app", "element_index"], "types": { "action": "string", "app": "string", "element_index": "string" }, "enums": {}, "readOnly": false },
    { "name": "set_value", "description": "Set the value of a settable accessibility element", "required": ["app", "element_index", "value"], "properties": ["app", "element_index", "value"], "types": { "app": "string", "element_index": "string", "value": "string" }, "enums": {}, "readOnly": false },
    { "name": "select_text", "description": "Select text inside a text element, or place the text cursor before or after it. Provide text exactly as it appears in the accessibility tree, including any Markdown formatting. If the text is not unique, provide surrounding prefix or suffix text to disambiguate it.", "required": ["app", "element_index", "text"], "properties": ["app", "element_index", "prefix", "selection", "suffix", "text"], "types": { "app": "string", "element_index": "string", "prefix": "string", "selection": "string", "suffix": "string", "text": "string" }, "enums": { "selection": ["text", "cursor_before", "cursor_after"] }, "readOnly": false },
    { "name": "scroll", "description": "Scroll an element in a direction by a number of pages", "required": ["app", "direction", "element_index"], "properties": ["app", "direction", "element_index", "pages"], "types": { "app": "string", "direction": "string", "element_index": "string", "pages": "number" }, "enums": {}, "readOnly": false },
    { "name": "drag", "description": "Drag from one point to another using pixel coordinates", "required": ["app", "from_x", "from_y", "to_x", "to_y"], "properties": ["app", "from_x", "from_y", "to_x", "to_y"], "types": { "app": "string", "from_x": "number", "from_y": "number", "to_x": "number", "to_y": "number" }, "enums": {}, "readOnly": false },
    { "name": "press_key", "description": "Press a key or key-combination on the keyboard, including modifier and navigation keys.\n  - This supports xdotool's `key` syntax.\n  - Examples: \"a\", \"Return\", \"Tab\", \"super+c\", \"Up\", \"KP_0\" (for the numpad 0 key).", "required": ["app", "key"], "properties": ["app", "key"], "types": { "app": "string", "key": "string" }, "enums": {}, "readOnly": false },
    { "name": "type_text", "description": "Type literal text using keyboard input", "required": ["app", "text"], "properties": ["app", "text"], "types": { "app": "string", "text": "string" }, "enums": {}, "readOnly": false }
  ]
}
