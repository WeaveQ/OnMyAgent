link-workspace-packages=true
prefer-workspace-packages=true

# Electron binary download settings used by local install and CI mirrors.
# Keep this version aligned with apps/desktop/package.json devDependencies.electron.
electron_mirror="https://cdn.npmmirror.com/binaries/electron/"
electron_custom_dir="v39.8.10"
